/** Renderer-facing labels for Sui dapp signing methods (main signing lives in main/browser). */
export const SUI_DAPP_SIGN_METHODS = [
  "sui:signPersonalMessage",
  "sui:signTransaction",
  "sui:signAndExecuteTransaction",
] as const;
