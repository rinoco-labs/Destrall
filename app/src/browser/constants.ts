/** Sentinel URL stored on home / new-tab rows — never loaded in the native WebView. */
export const BROWSER_HOME_URL = "destrall://home";

export const BROWSER_HISTORY_LIMIT = 20;

export const BROWSER_SEARCH_PLACEHOLDER = "Search or enter website URL";
