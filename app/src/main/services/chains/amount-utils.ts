/**
 * @deprecated Import from `shared/tokens/amounts` instead.
 * Kept for existing import paths during migration.
 */
export { parseTokenAmount as decimalStringToRawAmount } from "../../../shared/tokens/amounts";
